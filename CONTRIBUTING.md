# Contributing

1. Create a focused branch from `main`.
2. Install development dependencies with `pip install -e ".[dev]"`.
3. Add or update tests for behavior changes.
4. Run `ruff check .`, `ruff format --check .`, and `pytest`.
5. Open a pull request explaining the problem, approach, and verification.

Do not include credentials, private application data, or generated database files in commits.
