services:
  tracker:
    build: .
    ports:
      - "8000:8000"
    environment:
      DATABASE_URL: sqlite:////data/applications.db
    volumes:
      - tracker-data:/data

volumes:
  tracker-data:
